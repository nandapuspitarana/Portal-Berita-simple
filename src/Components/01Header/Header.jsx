import React from 'react';

const gaya = {
  margin: '0px',
  textAlign: 'center',
  backgroundColor: '#eee'
};

const Header = () => {
  return (
    <nav style={gaya}>
      <h1>Header</h1>
    </nav>
  );
};

export default Header;
